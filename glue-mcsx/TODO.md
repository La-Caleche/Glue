- [ ] Bugs
  - [ ] **`min-w`/`max-w` dead on `<overlay>` and root** — `overlayLayoutParams`/`rootLayoutParams` build `FrameLayout.LayoutParams` (no min/max fields) and never call `setMinimumWidth`. `select.mcsx:16` ships `min-w-[180px]` (does nothing); `docs/components.md:238` documents it as working.
  - [ ] Parent orientation handed to children is a build-time snapshot while `applyContainer` flips it reactively — a reactive `flex-row`↔`flex-col` mis-centers `mx-auto`/`my-auto` children. `buildScroll` hard-codes COLUMN.
  - [ ] `ElementResolver.stateWriter` captures `value` at build time — latent one-shot for `value={ref}`; every shipped use is a literal.
  - [ ] Variant maps replace rather than merge per state.
  - [ ] Arbitrary values are inconsistent: `w-[120px]`/`min-w-[80px]`/`inset-[3px]`/`rounded-[8px]`/`bg-[#hex]`/`opacity-[.5]` work, but **`p-[10px]` and `gap-[10px]` throw** (padding/gap route through `scaled()` → `requireInt()`).
  - [ ] `selection-*` / `scrollbar-*` are accepted and silently dropped (`TailwindParser.applyPrefixed`) — every other unknown class gets a warn-once. Warn or throw.
- [ ] Refacto
  - [ ] Check if `src/main/java/fr/lacaleche/mcsx/core/layout/FlexEngine.java` can be merged to Yoga — *assessed: Yoga is in no config today, so this adds a native JNI dep inside `core.*` (headless boundary) and would rewrite `FlexLayout` too, against 291 pure-`int` lines with ~30 tests. Discuss.*
  - [ ] Abstract `src/main/java/fr/lacaleche/mcsx/core/lint/McsxLinter.java` — *assessed: already 6 named checks, but they share the mutable `scope` deque + controller-reflection helpers, so a `Rule` registry needs a context object to buy anything. Discuss.*
  - [ ] Need `src/main/java/fr/lacaleche/mcsx/core/mcsx/` to be properly data-driven with a cleaner loading API — *assessed: 3 entry points, injectable `ClassLoader`, tested, cached. The concrete lever is a `Function<String, InputStream>` source if a second origin (datapack/disk) is wanted. Discuss.*
  - [ ] Rewrite `src/main/java/fr/lacaleche/mcsx/core/style/` and `src/main/java/fr/lacaleche/mcsx/core/theme/` to a cleaner, more abstract, code — *assessed: 67 tests / 1015 test lines vs 1672 source; `StyleSpec`'s nested records are a deliberate type-safety tradeoff. Concrete lever: collapse `applyPrefixed`'s padding/inset/min-max runs (`TailwindParser:261-321`) to a prefix table. Discuss.*
  - [ ] **`Themes.ACTIVE` is process-global mutable state** (`core/theme/Themes.java:73`) — and the tests already pay for it: `ThemeTest:24`, `OverlayScrimThemeTest:112,117`, `DockThemeRepaintTest:115,120` each defensively reset it, so a test that forgets breaks another depending on JUnit order. It sits in `core.*`, whose whole point is headless testability. Fix = thread a `Theme` through the bind context, keep `Themes.active()` as the convenience default. Ripples into `view.*` — real work, not a cleanup.
  - [ ] `ReactiveRuntime.STACK` is a static non-thread-safe `ArrayDeque` on the hottest path. The single-threaded contract is documented (`:9`), but a `Signal.set` off the UI thread corrupts it silently. Cheap insurance if wanted: a `Thread.currentThread()` assert in `push` only (never in `onRead`).
- [ ] Optimize
  - [ ] Optimize `src/main/java/fr/lacaleche/mcsx/viewport/`
  - [ ] `resolveStyle` runs **3× per element** (and 3× the interpolation work per component, where the `{class}` hole defeats the parser cache) — should be 1×.
  - [ ] `StyleSpec` allocates on the empty path: `rawAttributeStyle` builds 7 objects for an element declaring none of its 8 attributes, and `merged` never short-circuits — ~13 objects per `resolveStyle`, ~51 per `<div>`. A `StyleSpec.EMPTY` + short-circuit takes the common path to zero allocations.
  - [ ] `BindingResolver` re-does `getDeclaredField`/`setAccessible` on **every** read — memoize the reflection lookups.
  - [ ] `bindParts` — guard `TextView.setText` with a `setTextIfChanged`.
  - [ ] `bindLayoutParams` still allocates a throwaway `Effect` + two `LinkedHashSet`s per child per build (the retain cost is gone, the alloc isn't).
- [ ] Docs
  - [ ] `docs/tailwind-compatibility.md` — the score (15%) is sound, but: "grow, and shrink" under *Strongest Coverage* is now TRUE for `grow-0` (fixed) — re-check the family; "last utility wins" is false for corners (`cornerRadii()` resolves per-corner before uniform, so `rounded-t-md rounded` does not round uniformly); `rounded-sm`/`rounded-none` are now supported in the uniform form and need re-scoring; the arbitrary-value and negative-value (`-mt-2`, `-top-2` all throw) gaps are unlisted; margin is scored "partial" on the strength of `m-auto` alone (no `m-4`, no `mt-2`, no `space-x-*`); the common typography misses (`uppercase`, `underline`, `truncate`, `leading-*`, `tracking-*`, `font-mono`; sizes stop at `2xl`) go unnamed. *Intentional Differences* should also cover what **looks** supported and isn't.
  - [ ] CLAUDE.md / AGENTS.md still say `view.*` is compile-verified only — false since `8e63844`: a `Context` stub returning `Resources.getSystem()` + `newTheme()` builds the whole View tree in JUnit (see `GateSubscriptionTest`, `ViewStylesTest`). Update the rule so future work writes view tests instead of deferring to in-game.
  - [ ] `build.gradle.kts` — the ModernUI/Arc3D exclusions MUST stay per-dependency. Two tidier shapes are dead ends and should not be "fixed" back: (1) a configuring closure over a catalog **bundle** throws *"Minimal dependencies are immutable"* at configuration time; (2) hoisting the excludes onto a bucket `Configuration` that `implementation` extends **compiles but breaks the build** — exclude rules are inherited across `extendsFrom` and strip slf4j/log4j/annotations from Minecraft's own graph too (29 compile errors in `view.dock`, `viewport`, `mui`).
  - [ ] Release notes for the next publish: `StyleSpec.Flex` record shape changed; 3-arg `ViewStyles.applyBox(true)` now also zeroes padding/resets alpha; 2-arg `applyText` changed gravity behaviour and has zero in-repo callers (external-only surface, untested).
- [ ] Verify & land
  - [ ] **Component library moved `src/main` → `src/testmod`** (31 `.mcsx` files, now untracked; `ComponentLibraryTest` repointed; `view/component/StandardComponents.java` + `client/McsxClient.java` deleted). Suite is green. Loose ends: the new `src/testmod/resources/assets/mcsx/ui/components/` is untracked and needs `git add`; `docs/components.md` and any `mcsx:components/*` references still describe them as shipped library content; decide whether consumers (ignis-editor) relied on the shipped copies, since this is a breaking change for them if so.
  - [ ] `8e63844` (amend of `71f3ae1`) is LOCAL ONLY — origin/main still has `71f3ae1`, so publishing needs `git push --force-with-lease`. Decide. `design-poc/` + `design-poc-screens/` still untracked, undecided.
  - [ ] In-game pass (`testmodClient`) — still needed for what the headless tests can't see (GL, real input, focus). Checklist: `/mcsx dock` float layout survives a resolution change; no off-stage first frame; no drag dead zone; panes cascade from centre (not `(20,20)`); two dropdowns open independently; a lazily-bound pane doesn't steal `<input>` focus; a duplicate `<key combo>` fails with the `.mcsx` line/column; `w-1/2` in a nested auto-sized `<row>` renders at half width; `w={progress}` at `0f` collapses. Plus from the reactive fixes: glass segmented control + editor tab strip labels recolour on click; theme toggle with a modal open repaints the scrim; ticking one checkbox in a list leaves the other rows' state intact.
