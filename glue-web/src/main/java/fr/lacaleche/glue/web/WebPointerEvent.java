package fr.lacaleche.glue.web;

/** Pointer events in browser-pixel coordinates, including movement outside a captured drag. */
public enum WebPointerEvent {
    MOVED,
    EXITED,
    PRESSED,
    RELEASED,
    DRAGGED
}
